export default function Button({label}) {
    return (
        <div>
            <a className="btn btn-primary btn-xl text-uppercase" href="#services">
            {label}
          </a>
        </div>
    )
}
